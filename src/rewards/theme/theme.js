import { createTheme } from "@mui/material";


const theme = createTheme({
  
  palette: {
    primary: {
      main: '#DD212A', // Replace with your primary color
    },
    secondary: {
      main: '#F78121', // Replace with your secondary color
    },
    black:{
        main:'#000'
    }
    // Add more palette options as needed
  },
  typography: {
    // Customize typography options if needed
  },
  
  // Add more theme options as needed
});

export default theme;
